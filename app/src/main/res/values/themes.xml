<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme used before Compose takes over rendering. -->
    <style name="Theme.YourDailyImprovement" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/brand_background</item>
        <item name="android:statusBarColor">@color/brand_background</item>
    </style>
</resources>
